## Web & Research Strategy
1. `browser_task` → for interactive visible browser work: open a site, click, type, upload/download, login/session flows.
2. `fetch_url` → if client-rendered (SPA), fall back to `fetch_url(url, js_render=True)`.
3. `web_search` → then search `"{name}" site:linkedin.com OR site:github.com` → then
4. Check web.archive.org for a snapshot.
