---
title: CLI
description: Koza CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The Koza CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
koza
```

But it also accepts commands as documented on this page. This allows you to interact with Koza programmatically.

```bash
koza run "Explain how closures work in JavaScript"
```

---

### tui

Start the Koza terminal user interface.

```bash
koza [project]
```

#### Flags

| Flag                                        | Short | Description                                                             |
| ------------------------------------------- | ----- | ----------------------------------------------------------------------- |
| <nobr><code>{"--continue"}</code></nobr>    | `-c`  | Continue the last session                                               |
| <nobr><code>{"--session"}</code></nobr>     | `-s`  | Session ID to continue                                                  |
| <nobr><code>{"--fork"}</code></nobr>        |       | Fork the session when continuing (use with `--continue` or `--session`) |
| <nobr><code>{"--prompt"}</code></nobr>      |       | Prompt to use                                                           |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use in the form of provider/model                              |
| <nobr><code>{"--agent"}</code></nobr>       |       | Agent to use                                                            |
| <nobr><code>{"--auto"}</code></nobr>        |       | Auto-approve permissions that are not explicitly denied                 |
| <nobr><code>{"--port"}</code></nobr>        |       | Port to listen on                                                       |
| <nobr><code>{"--hostname"}</code></nobr>    |       | Hostname to listen on                                                   |
| <nobr><code>{"--mdns"}</code></nobr>        |       | Enable mDNS discovery                                                   |
| <nobr><code>{"--mdns-domain"}</code></nobr> |       | Custom mDNS domain name                                                 |
| <nobr><code>{"--cors"}</code></nobr>        |       | Additional browser origin(s) to allow CORS                              |

---

## Commands

The Koza CLI also has the following commands.

---

### agent

Manage agents for Koza.

```bash
koza agent [command]
```

---

#### create

Create a new agent with custom configuration.

```bash
koza agent create
```

This command will guide you through creating a new agent with a custom system prompt and permission configuration. Anything you don't allow is denied in the generated agent's frontmatter.

#### Flags

| Flag                                        | Short | Description                                                                                                                                                                                                                |
| ------------------------------------------- | ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| <nobr><code>{"--path"}</code></nobr>        |       | Directory to write the agent file to (defaults to global or `.koza/agent` based on the prompt)                                                                                                                         |
| <nobr><code>{"--description"}</code></nobr> |       | What the agent should do                                                                                                                                                                                                   |
| <nobr><code>{"--mode"}</code></nobr>        |       | Agent mode: `all`, `primary`, or `subagent`                                                                                                                                                                                |
| <nobr><code>{"--permissions"}</code></nobr> |       | Comma-separated list of permissions to allow (default: all). Available: `bash`, `read`, `edit`, `glob`, `grep`, `webfetch`, `task`, `todowrite`, `websearch`, `lsp`, `skill`. Anything omitted is denied. Alias: `--tools` |
| <nobr><code>{"--model"}</code></nobr>       | `-m`  | Model to use, in `provider/model` format                                                                                                                                                                                   |

Passing all of `--path`, `--description`, `--mode`, and `--permissions` runs the command non-interactively.

---

#### list

List all available agents.

```bash
koza agent list
```

---

### attach

Attach a terminal to an already running Koza backend server started via `serve` or `web` commands.

```bash
koza attach [url]
```

This allows using the TUI with a remote Koza backend. For example:

```bash
# Start the backend server for web/mobile access
koza web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
koza attach http://10.20.30.40:4096
```

#### Flags

| Flag                                     | Short | Description                                                                |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--dir"}</code></nobr>      |       | Working directory to start TUI in                                          |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Basic auth password (defaults to `KOZA_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Basic auth username (defaults to `KOZA_SERVER_USERNAME` or `koza`) |

---

### auth

Command to manage credentials and login for providers.

```bash
koza auth [command]
```

---

#### login

Koza is powered by the provider list at [Models.dev](https://models.dev), so you can use `koza auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/koza/auth.json`.

```bash
koza auth login
```

When Koza starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

##### Flags

| Flag                                     | Short | Description                                          |
| ---------------------------------------- | ----- | ---------------------------------------------------- |
| <nobr><code>{"--provider"}</code></nobr> | `-p`  | Provider ID or name to log in to                     |
| <nobr><code>{"--method"}</code></nobr>   | `-m`  | Login method label to use, skipping method selection |

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
koza auth list
```

Or the short version.

```bash
koza auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
koza auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
koza github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
koza github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
koza github run
```

##### Flags

| Flag                                  | Description                            |
| ------------------------------------- | -------------------------------------- |
| <nobr><code>{"--event"}</code></nobr> | GitHub mock event to run the agent for |
| <nobr><code>{"--token"}</code></nobr> | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
koza mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
koza mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
koza mcp list
```

Or use the short version.

```bash
koza mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
koza mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
koza mcp auth list
```

Or use the short version.

```bash
koza mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
koza mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
koza mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
koza models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
koza models anthropic
```

#### Flags

| Flag                                    | Description                                                  |
| --------------------------------------- | ------------------------------------------------------------ |
| <nobr><code>{"--refresh"}</code></nobr> | Refresh the models cache from models.dev                     |
| <nobr><code>{"--verbose"}</code></nobr> | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in Koza.

```bash
koza models --refresh
```

---

### run

Run koza in non-interactive mode by passing a prompt directly.

```bash
koza run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "koza run"
koza run Explain the use of context in Go
```

You can also attach to a running `koza serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
koza serve

# In another terminal, run commands that attach to it
koza run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag                                     | Short | Description                                                                |
| ---------------------------------------- | ----- | -------------------------------------------------------------------------- |
| <nobr><code>{"--command"}</code></nobr>  |       | The command to run, use message for args                                   |
| <nobr><code>{"--continue"}</code></nobr> | `-c`  | Continue the last session                                                  |
| <nobr><code>{"--session"}</code></nobr>  | `-s`  | Session ID to continue                                                     |
| <nobr><code>{"--fork"}</code></nobr>     |       | Fork the session when continuing (use with `--continue` or `--session`)    |
| <nobr><code>{"--share"}</code></nobr>    |       | Share the session                                                          |
| <nobr><code>{"--model"}</code></nobr>    | `-m`  | Model to use in the form of provider/model                                 |
| <nobr><code>{"--agent"}</code></nobr>    |       | Agent to use                                                               |
| <nobr><code>{"--file"}</code></nobr>     | `-f`  | File(s) to attach to message                                               |
| <nobr><code>{"--format"}</code></nobr>   |       | Format: default (formatted) or json (raw JSON events)                      |
| <nobr><code>{"--title"}</code></nobr>    |       | Title for the session (uses truncated prompt if no value provided)         |
| <nobr><code>{"--attach"}</code></nobr>   |       | Attach to a running koza server (e.g., http://localhost:4096)          |
| <nobr><code>{"--password"}</code></nobr> | `-p`  | Basic auth password (defaults to `KOZA_SERVER_PASSWORD`)               |
| <nobr><code>{"--username"}</code></nobr> | `-u`  | Basic auth username (defaults to `KOZA_SERVER_USERNAME` or `koza`) |
| <nobr><code>{"--dir"}</code></nobr>      |       | Directory to run in, or path on the remote server when attaching           |
| <nobr><code>{"--port"}</code></nobr>     |       | Port for the local server (defaults to random port)                        |
| <nobr><code>{"--variant"}</code></nobr>  |       | Model variant (provider-specific reasoning effort)                         |
| <nobr><code>{"--thinking"}</code></nobr> |       | Show thinking blocks                                                       |
| <nobr><code>{"--auto"}</code></nobr>     |       | Auto-approve permissions that are not explicitly denied                    |

---

### serve

Start a headless Koza server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
koza serve
```

This starts an HTTP server that provides API access to koza functionality without the TUI interface. Set `KOZA_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `koza`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### session

Manage Koza sessions.

```bash
koza session [command]
```

---

#### list

List all Koza sessions.

```bash
koza session list
```

##### Flags

| Flag                                      | Short | Description                          |
| ----------------------------------------- | ----- | ------------------------------------ |
| <nobr><code>{"--max-count"}</code></nobr> | `-n`  | Limit to N most recent sessions      |
| <nobr><code>{"--format"}</code></nobr>    |       | Output format: table or json (table) |

---

#### delete

Delete an Koza session.

```bash
koza session delete <sessionID>
```

---

### stats

Show token usage and cost statistics for your Koza sessions.

```bash
koza stats
```

#### Flags

| Flag                                    | Description                                                                 |
| --------------------------------------- | --------------------------------------------------------------------------- |
| <nobr><code>{"--days"}</code></nobr>    | Show stats for the last N days (all time)                                   |
| <nobr><code>{"--tools"}</code></nobr>   | Number of tools to show (all)                                               |
| <nobr><code>{"--models"}</code></nobr>  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| <nobr><code>{"--project"}</code></nobr> | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
koza export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

#### Flags

| Flag                                     | Description                           |
| ---------------------------------------- | ------------------------------------- |
| <nobr><code>{"--sanitize"}</code></nobr> | Redact sensitive transcript/file data |

---

### import

Import session data from a JSON file or Koza share URL.

```bash
koza import <file>
```

You can import from a local file or an Koza share URL.

```bash
koza import session.json
koza import https://opncd.ai/s/abc123
```

---

### web

Start a headless Koza server with a web interface.

```bash
koza web
```

This starts an HTTP server and opens a web browser to access Koza through a web interface. Set `KOZA_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `koza`).

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
koza acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag                                        | Description                                |
| ------------------------------------------- | ------------------------------------------ |
| <nobr><code>{"--cwd"}</code></nobr>         | Working directory                          |
| <nobr><code>{"--port"}</code></nobr>        | Port to listen on                          |
| <nobr><code>{"--hostname"}</code></nobr>    | Hostname to listen on                      |
| <nobr><code>{"--mdns"}</code></nobr>        | Enable mDNS discovery                      |
| <nobr><code>{"--mdns-domain"}</code></nobr> | Custom mDNS domain name                    |
| <nobr><code>{"--cors"}</code></nobr>        | Additional browser origin(s) to allow CORS |

---

### plugin

Install a plugin and update your config.

```bash
koza plugin <module>
```

Or use the alias.

```bash
koza plug <module>
```

#### Flags

| Flag                                   | Short | Description                     |
| -------------------------------------- | ----- | ------------------------------- |
| <nobr><code>{"--global"}</code></nobr> | `-g`  | Install in global config        |
| <nobr><code>{"--force"}</code></nobr>  | `-f`  | Replace existing plugin version |

---

### pr

Fetch and checkout a GitHub PR branch, then run Koza.

```bash
koza pr <number>
```

---

### db

Database tools.

```bash
koza db [query]
```

#### Flags

| Flag                                   | Description                    |
| -------------------------------------- | ------------------------------ |
| <nobr><code>{"--format"}</code></nobr> | Output format: `json` or `tsv` |

---

#### path

Print the database path.

```bash
koza db path
```

---

### debug

Debugging and troubleshooting tools.

```bash
koza debug [command]
```

---

### uninstall

Uninstall Koza and remove all related files.

```bash
koza uninstall
```

#### Flags

| Flag                                        | Short | Description                                 |
| ------------------------------------------- | ----- | ------------------------------------------- |
| <nobr><code>{"--keep-config"}</code></nobr> | `-c`  | Keep configuration files                    |
| <nobr><code>{"--keep-data"}</code></nobr>   | `-d`  | Keep session data and snapshots             |
| <nobr><code>{"--dry-run"}</code></nobr>     |       | Show what would be removed without removing |
| <nobr><code>{"--force"}</code></nobr>       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates koza to the latest version or a specific version.

```bash
koza upgrade [target]
```

To upgrade to the latest version.

```bash
koza upgrade
```

To upgrade to a specific version.

```bash
koza upgrade v0.1.48
```

#### Flags

| Flag                                   | Short | Description                                                       |
| -------------------------------------- | ----- | ----------------------------------------------------------------- |
| <nobr><code>{"--method"}</code></nobr> | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The koza CLI takes the following global flags.

| Flag                                       | Short | Description                          |
| ------------------------------------------ | ----- | ------------------------------------ |
| <nobr><code>{"--help"}</code></nobr>       | `-h`  | Display help                         |
| <nobr><code>{"--version"}</code></nobr>    | `-v`  | Print version number                 |
| <nobr><code>{"--print-logs"}</code></nobr> |       | Print logs to stderr                 |
| <nobr><code>{"--log-level"}</code></nobr>  |       | Log level (DEBUG, INFO, WARN, ERROR) |
| <nobr><code>{"--pure"}</code></nobr>       |       | Run without external plugins         |

---

## Environment variables

Koza can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `KOZA_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `KOZA_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `KOZA_CONFIG`                     | string  | Path to config file                               |
| `KOZA_TUI_CONFIG`                 | string  | Path to TUI config file                           |
| `KOZA_CONFIG_DIR`                 | string  | Path to config directory                          |
| `KOZA_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `KOZA_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `KOZA_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `KOZA_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `KOZA_PERMISSION`                 | string  | Inlined json permissions config                   |
| `KOZA_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `KOZA_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `KOZA_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `KOZA_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `KOZA_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `KOZA_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `KOZA_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `KOZA_DISABLE_MODELS_FETCH`       | boolean | Disable fetching models from remote sources       |
| `KOZA_DISABLE_MOUSE`              | boolean | Disable mouse capture in the TUI                  |
| `KOZA_FAKE_VCS`                   | string  | Fake VCS provider for testing purposes            |
| `KOZA_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `KOZA_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `KOZA_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `KOZA_SERVER_USERNAME`            | string  | Override basic auth username (default `koza`) |
| `KOZA_MODELS_URL`                 | string  | Custom URL for fetching models configuration      |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `KOZA_EXPERIMENTAL`                         | boolean | Enable the experimental umbrella flag   |
| `KOZA_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `KOZA_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `KOZA_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `KOZA_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `KOZA_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `KOZA_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `KOZA_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
| `KOZA_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disable file watcher                    |
| `KOZA_EXPERIMENTAL_EXA`                     | boolean | Enable experimental Exa features        |
| `KOZA_EXPERIMENTAL_LSP_TY`                  | boolean | Enable TY LSP for python files          |
| `KOZA_EXPERIMENTAL_PLAN_MODE`               | boolean | Enable plan mode                        |
| `KOZA_EXPERIMENTAL_BACKGROUND_SUBAGENTS`    | boolean | Enable background subagent tasks        |
| `KOZA_EXPERIMENTAL_EVENT_SYSTEM`            | boolean | Enable experimental event system        |
| `KOZA_EXPERIMENTAL_NATIVE_LLM`              | boolean | Enable native LLM request path          |
| `KOZA_EXPERIMENTAL_PARALLEL`                | boolean | Enable parallel web search execution    |
| `KOZA_EXPERIMENTAL_SCOUT`                   | boolean | Enable Scout subagent                   |
| `KOZA_EXPERIMENTAL_WORKSPACES`              | boolean | Enable workspace support                |
